//function

// 1 function without argum. & without return type
// 2 function with argm & without return type
// 3 function without argum. & with return type
// 4 function with argm & with return type


    function add(){
        console.log("U are In Function");        
    }

    // add();

    function add1(a:number,b:number):number{
        return (a+b);
    }

    // var temp=add1(2,3);
    // console.log("Addition is "+temp);

    function add3(a:number,b:number){
        console.log("Addition is "+(a+b));
    }

    // add3(4,4);

    //annonymous function

    

